/**
 * Families and groups remain in the persistence model for backwards
 * compatibility, but are intentionally absent from every catalog UI.
 * Re-enable this switch to restore those surfaces without a data migration.
 */
export const catalogFamilyGroupUiEnabled = false;
